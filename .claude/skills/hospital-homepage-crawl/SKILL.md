---
name: hospital-homepage-crawl
description: 미용·성형·피부과 병원의 공식 홈페이지를 Playwright MCP로 직접 방문해 운영정보·의료진·제품·장비·시술·가격을 추출하고, 에스테틱 제품/장비 카탈로그에 매칭한 정규화 JSON을 만든다.
---

# 병원 홈페이지 크롤링

단일 병원의 **공식 홈페이지에서만** 데이터를 수집해 PRD §6.1 스키마의 정규화 JSON을 만든다.
검색엔진은 URL을 찾는 용도로만 쓰고, 블로그·SNS·지도는 데이터 추출 대상이 아니다.

## 배경 및 목적

### 배경
플랜닥스는 에스테틱 제품(톡신, 필러, 스킨부스터)을 미용·성형 병원에 판매하는 사업을 운영한다. 영업 및 마케팅 전략 수립을 위해 타깃병원(피부과, 클리닉의원, 성형외과)의 상세 정보가 필요하지만, 현재 공공DB에서 확보한 기본정보(이름, 주소, 좌표)만 보유하고 있다.

### 목적
**병원 1개의 공식 홈페이지를 자동 크롤링하여**, 운영정보·의료진·취급 제품·장비·시술·다국어 지원 여부를 수집하고, 관계형 DB에 적재할 수 있는 정규화된 JSON으로 출력한다. 본 단계의 목표는 **한 병원에 대해 신뢰할 수 있는 완성도·정확도를 달성**하는 것이며, 이 품질이 검증된 후 처리 대상을 확대한다.

### 기대 효과
- 병원별 취급 제품·장비 현황 파악 → 영업 타깃팅 정밀화
- 경쟁사 제품 침투율 분석 → 시장 점유율 전략 수립
- 시술 가격 분포 분석 → 가격 정책 수립 근거
- 다국어 지원 병원 식별 → 해외 마케팅 관심 병원 발굴

## 입력
- 병원 ID, 병원명, 시도, 시군구, 읍면동, 도로명주소, 위도, 경도, 홈페이지 URL
- 참조 카탈로그: [aesthetic_products.json](reference/aesthetic_products.json), [aesthetic_equipments.json](reference/aesthetic_equipments.json)

## 출력
추후 관계형 DB 저장에 용이하도록 정규화 된 형태의 JSON 파일로 저장한다.
- 스키마: [output_scheme.py](reference/output_scheme.py)
- 샘플 Output: [sample_output.json](reference/sample_output.json)
- 저장위치 및 형식: `/output/{병원이름}_{병원ID}.json`

## 절차

### 1. URL 확보
- 홈페이지 URL이 주어지면 병원명, 주소 등의 정보로 검증 후 사용한다. 만약 잘못된 URL로 판단되면 WebSearch를 통해 찾는다.
- 없으면 WebSearch로 후보를 찾고 Playwright로 접속해 병원명·주소 일치를 확인한다. URL 확보는 검색 허용, 단 **데이터는 홈페이지에서만** 뽑는다.

### 2. 페이지 라우팅 (방문 순서)
홈페이지 구조는 제각각이지만 정보 종류별로 다음을 반드시 확인한다.
1. **home** — 전체 네비게이션 파악, 다국어 스위처 유무
2. **병원 소개 / story, intro, doctor 등** — 운영정보(대표·사업자번호·전화·영업시간·주차·교통·시설) + 의료진
3. **시술·장비 목록 / contents 등** — 제품·장비명 수집
4. **시술 상세 페이지** — ⚠️ 반드시 본문 진입. 장비명이 목록이 아니라 **시술 설명 안에만** 있는 경우가 있다(예: "우마카세 리프팅" 설명 속 티타늄2·울쎄라2프라임).
5. **가격 + 비급여진료(안내) / event, promotion 등** — 가격 확인. ⚠️ 성형외과·외과 계열은 이벤트 페이지에 가격이 없어도 **"비급여진료비용 안내"** 페이지(법정 고지 의무)에 일부 시술 가격이 **텍스트**로 있다. 푸터/하단 링크에서 `비급여`·`nonpay`·`진료비용`을 찾아 반드시 방문한다. (4월31일성형외과 사례: 이벤트엔 가격 없었으나 비급여 페이지에 비밸브수술·상담료·3D-CT 가격이 텍스트로 존재)

각 페이지에서 탭·아코디언·"더보기"를 클릭해 숨은 콘텐츠까지 펼친다. 동적 로딩은 `browser_wait_for`.

> **별도 도메인 분원 처리**: 비수술(쁘띠/필러·톡신·레이저) 부문이나 특정 시술이 **다른 도메인**으로 운영되는 경우가 있다(예: 4월31일의 쁘띠센터 = `ultimateliftingprojets.com`). 홈페이지-한정 정책상 기본은 **크롤링 범위 외**로 두되, 제품·장비 매칭 대상이 거기 몰려 있을 수 있으므로 `crawl_metadata.notes`에 도메인과 "동일 병원으로 묶을지 정책 결정 필요"를 반드시 기록한다. 임의로 크롤링하거나 무시하지 말 것.

### 3. 추출
원문 그대로 추출하고 번역·요약·정규화는 나중에 한다.

### 4. 카탈로그 매칭 — 핵심
단순 substring 매칭은 약하다. 다음 규칙을 모두 적용한다.

- **한↔영 교차 매칭**: 사이트 표기 ↔ 카탈로그의 한글/영문명을 양방향으로 대조.
  - `에코2`/`Eco 2` → `eCO2(이코투)`
  - `Co2 레이저` → `CO₂ 프락셔널`
  - `테오시알`/`Teosyal` → `테옥산 RHA`
- **유니코드 정규화(NFKC)**: `CO₂`↔`CO2`, 전각↔반각, 첨자 통일 후 비교.
- **수식어·접미사 제거**: `디스커버리 루비` → `루비 레이저`, `슈링크 유니버셜` → `슈링크`, 모델 넘버(`인모드 포마/FX`) 유연 처리.
- **시술-장비 연결 추출**: 시술 설명 안의 장비명도 equipment로 잡고, treatment의 `equipment_name`에 연결한다.
- **본문 속 제품·브랜드 열거**: 시술 설명·상세 본문 안에 나열된 개별 제품/브랜드명(특히 실리프팅 봉합사: 실루엣·울트라V리프트·브이락·POD·오메가/테스/미스코/금실 등)을 시술명 하나로 뭉뚱그리지 말고 **각각** 추출한다. 카탈로그에 없으면(봉합사·지방분해 등은 카탈로그 범위 밖) 버리지 말고 `unmatched_products`/`unmatched_equipments`에 개별 항목으로 기록한다. ⚠️ 자동 크롤링이 시술명만 잡고 본문 속 브랜드 열거를 누락하는 사고가 있었음(4월31일 실리프팅 봉합사 5종 누락).
- **무누락 원칙**: 매칭 실패해도 버리지 말고 `unmatched_*`에 `raw_name`·`category_guess`·`context`로 기록한다.
- **보수적 판단**: 발음만 비슷하고 동일 확신이 없으면 매칭하지 말고 unmatched로 두되 context에 의심 근거를 남긴다(예: `노블렉스` vs 카탈로그 `Nordlys` — 보류).
- **브랜드 다중 라인**: 브랜드만 있고 세부 라인이 불분명하면 사이트의 카테고리 맥락에 맞는 대표 제품으로 매칭(예: 필러 섹션의 `벨로테로` → `벨로테로 밸런스`).

코드 파이프라인으로 돌릴 때는 `lookup_product`/`lookup_equipment` 도구를 **각 항목마다 반드시 호출**한다. 로직상 매칭 가능한데 도구 미호출로 unmatched가 되는 사고가 자동 크롤링에서 발생했다.

### 5. 가격
- 가격이 **이미지로만** 제공되면 텍스트 추출 불가 → `price`/`price_display` null, `notes`에 "이미지 가격" 명시. (OCR 없이는 구조적으로 불가능하며 프롬프트로 풀 문제 아님)
- 텍스트 가격은 숫자만 정수(원)로, 원문은 `price_display`에 보존, 이벤트가 여부 기록.
- 이벤트/시술 페이지에 가격이 없어도 **비급여진료비용 안내 페이지**(§2-5 라우팅 참조)는 텍스트 가격의 유일한 출처인 경우가 많으니 반드시 확인한다.

### 6. 정규화 포맷
- 전화 `02-0000-0000`, 사업자번호 `000-00-00000`
- 영업시간: 요일별 `open/close/break_start/break_end` (HH:MM, 휴무 null)
- 여러 페이지의 동일 제품/장비/의료진은 하나로 통합.

## 완전성 체크리스트
출력 `crawl_metadata.completeness`에 7항목을 채운다. 수집되면 true:
`operation_info / doctors / products / equipments / treatments / prices / language_support`

**`crawl_metadata.notes`는 비워두지 말 것.** 최종 JSON을 내기 전, 다음 판단 근거를 반드시 문장으로 남긴다:
- 가격: 텍스트/이미지/없음 구분과 출처 페이지
- 매칭: 한↔영 매칭으로 확정한 항목, 보류(unmatched)한 항목과 그 이유
- 별도 도메인·다국어 등 정책 결정이 필요한 사항(§2 라우팅의 별도 도메인 처리 참조)
- 미방문/미확인으로 남긴 영역
